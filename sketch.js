var video;
var detector;
var label = "Cargando..."

function setup() {
  createCanvas(400,450);
  video=createCapture(VIDEO);
  video.hide();
  video.
  createCapture();
  size(380, 320);
}

function draw(){
  ground("#821737");
  textSize();
  fill("white");
  
}

